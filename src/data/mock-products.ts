// Re-export from new mocks for backward compatibility
export type { Product, Category } from "@/data/mock-products";
export { mockProducts as products, mockCategories as categories, mockBanners as banners, mockDeals as deals, mockFeaturedProducts as featuredProducts, mockTrendingProducts as trendingProducts } from "@/mocks/mockProducts";
